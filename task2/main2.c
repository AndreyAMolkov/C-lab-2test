#define_CRT_NO_WINDOWS_WARNINGS
#include"task2.c"
#include<stdio.h>

int main()
{
	double value = 0; score = 0; secret = 0;
	printf("the programm made a secret number. Enter the number for answer :");
	scanf("%i", &value);
	while ( int(value) != {0 - 9})
	{
		printf("\nYou entered error data.\n the programm made a secret number. Enter the number for answer :");
		scanf("%i", &value);
	}
	turn = (turn(value, secret);
	while (turn == 0, score++)
	{
		turn = (turn(value, secret);
		if (turn > 0)
			printf("\ntrips: You input the number biger then the secret. \n repit enter ")

	}










}